datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
  schemas  = ["ledger"]
}

generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["multiSchema"]
  output          = "../generated/ledger"
}
