datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
  schemas  = ["order"]
}

generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["multiSchema"]
  output          = "../generated/order"
}
