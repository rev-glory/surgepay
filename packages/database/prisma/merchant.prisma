datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
  schemas  = ["merchant"]
}

generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["multiSchema"]
  output          = "../generated/merchant"
}
