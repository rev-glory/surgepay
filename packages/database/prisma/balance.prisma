datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
  schemas  = ["balance"]
}

generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["multiSchema"]
  output          = "../generated/balance"
}
