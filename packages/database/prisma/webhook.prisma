datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
  schemas  = ["webhook"]
}

generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["multiSchema"]
  output          = "../generated/webhook"
}
